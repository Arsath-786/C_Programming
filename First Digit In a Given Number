#include <stdio.h>
int main()
{
    int num,firstdig;
    printf("Enter the number:");
    scanf("%d",&num);
    while(num>=10)
    {
        num=num/10;
    }
    firstdig=num;
    printf("first digit = %d\n",firstdig);
    return 0;
}
